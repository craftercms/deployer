target:
  env: {{env}}
  siteName: {{site_name}}
  localRepoPath: {{site_name}}
  randomParam: {{random_param}}
  studioUrl: ""
  studioManagementToken: "defaultManagementToken"
  search:
    indexIdFormat: '%s'
  deployment:
    pipeline:
      - processorName: testProcessor

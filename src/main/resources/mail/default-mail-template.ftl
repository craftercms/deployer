A deployment in server ${serverName} has just finished:

Target ID: ${targetId}
Start: ${start}
End: ${end}
Status: ${status}

<#if outputAttached>
Attached you'll also find the full output of the deployment.
</#if>